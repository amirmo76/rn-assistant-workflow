points to improve and modify the workflow:

- important update to the flow of the workflow:
    - always write all the specs for every dependent comopnent. make sure nothing remains in term of specs. all the required components have specs and are ready.
    - then plans all of them.
    - then reads all the plans, respecting what is dependent of what creates the task list utilizing parallel markers for speed.
    - then executes tasks, reviews, approvals and so on.

- make sure the assistant calls worker agents for tasks. a worker agent does not pick a task itself.
- make sure the assistant utilizes multiple worker agents for parallel tasks.
- make sure tasks and plans status properly get updated.
- make sure moving to done directory is working correctly and after approval and done the plan get's cleaned up.